<header>
    <nav class="navbar bg-body-tertiary p-0">
      <div class="container-fluid d-flex justify-content-between align-items-middle" style="background-color: #FFE4C4;">
          <div class="align-self-start"> <!-- Align the image to the left -->
              <a class="navbar-brand" href="/homepage">
                  <img src="/img/logo-admin.png" style="max-height: 70px;" alt="">
              </a>
          </div>
          <div class=" me-5"> 
            <div class="d-flex gap-3 align-items-center">
              <div>
                <a class="text-decoration-none text-dark" href="/homepage">
                <span> Home</span>
                </a>
              </div>
              <div>
                <a class="text-decoration-none text-dark" href="/shop">
                <span> Shop</span>
                </a>
              </div>
              <form class="d-flex" role="search" id="searchForm">
                <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
                <button class="btn btn-outline-success" type="submit">Search</button>
              </form>
              <div>
                <a class="text-decoration-none text-dark" href="">
                <i class="fa-solid fa-heart text-danger " ></i>
                </a>
              </div>
              <div>
                <a class="text-decoration-none text-dark " href="/cart">
                <i class="fa-solid fa-cart-shopping text-primary"></i>
                </a>
              </div>
                <div>
                    <% if(user) { %>
                      <div class="dropdown">
                        <a href="#" class="dropdown-toggle text-decoration-none text-dark" id="usernameLink" data-bs-toggle="dropdown" aria-expanded="false">
                            <%= user.username %> <i class="fa-solid fa-user"></i>
                        </a>
                        <ul class="dropdown-menu" aria-labelledby="usernameLink">
                            <li><a class="dropdown-item" href="/profile">Profile</a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item" href="/logout" id="logoutLink">Logout</a></li>
                        </ul>
                    </div>
                      <%} else { %>
                        <a class="text-decoration-none text-dark" href="/signup">
                            <i class="fa-solid fa-user"></i><span>
                              signup
                          </a>
                        <%}%>
                    </span>
                  </a>
                </div>
              </div>
          </div>
      </div>
    </nav>
    <script>
document.addEventListener("DOMContentLoaded", function () {
  const searchForm = document.getElementById("searchForm");
  const searchInput = document.getElementById("searchInput");
  const searchResultsContainer = document.getElementById("searchResults");

  searchForm.addEventListener("submit", function (e) {
    e.preventDefault();

    const searchQuery = searchInput.value;

    // Make an AJAX request to the server
    fetch(`/search?q=${searchQuery}`)
      .then((response) => response.json())
      .then((data) => {
        // Handle the search results, e.g., display them in the searchResultsContainer
        const results = data.results;
        displaySearchResults(results);
      });
  });

  // Function to display search results (implement as needed)
  function displaySearchResults(results) {
    // Implement how you want to display search results on your web page
    // You can append results to the searchResultsContainer, for example.
  }
});
    </script>
    </header>
    